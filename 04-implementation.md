# 4 РЕАЛИЗАЦИЯ

> **TODO:** Глава будет написана после реализации обоих архитектурных вариантов и фиксации выбора по результатам экспериментов главы 5. Ниже приведён скелет подразделов в соответствии с планом.

## 4.1 Структура изменений в репозитории SPYT

> **TODO:** Перечень затронутых модулей: `data-source-extended`, `data-source`, `yt-wrapper`, `spark-adapter`, `spark-patch`. Краткое описание роли каждого модуля.

## 4.2 Контекст батча

> **TODO:** Описание класса `YtStreamingBatchContext`: хранение смещения конца батча и пути потребителя, реализация через `ThreadLocal`, жизненный цикл переменной в рамках одного батча.

## 4.3 Модификация потокового источника

### 4.3.1 Метод getBatch

> **TODO:** Описание изменений в `YtStreamingSource.getBatch` — запись смещения конца батча в `YtStreamingBatchContext` для последующего использования приёмником.

### 4.3.2 Метод commit

> **TODO:** Описание изменений в `YtStreamingSource.commit` — пропуск операции `advanceConsumer` в транзакционном режиме, поскольку смещение уже продвинуто в составе фиксированной транзакции приёмника.

## 4.4 Модификация потокового приёмника

> **TODO:** Описание изменений в `YtStreamingSink.addBatch` для выбранного варианта реализации.

## 4.5 Расширение YtDynamicTableWriter

> **TODO:** Описание перегрузки, принимающей `ApiServiceTransaction` в качестве параметра, и обеспечения совместимости со старым API.

## 4.6 Декоратор MicroBatchExecution

> **TODO:** Описание декоратора `MicroBatchExecutionDecorators`, использующего аннотацию `@Decorate` для подмены метода `runBatch`. Очистка `ThreadLocal`-контекста между батчами.

## 4.7 SPI-точка расширения

> **TODO:** Описание интерфейсов `StreamingTransactionSupport`, `StreamingTransactionHandle` и реализации `YTsaurusStreamingTransactionSupport`, регистрируемой через `META-INF/services`.

## 4.8 Конфигурация и обратная совместимость

> **TODO:** Описание конфигурационного флага включения транзакционного режима и поведения адаптера при отключённом флаге.

\newpage
