%%{init: {'theme': 'base', 'themeVariables': { 'background': '#ffffff', 'primaryColor': '#ffffff', 'primaryBorderColor': '#000000', 'primaryTextColor': '#000000', 'lineColor': '#000000', 'signalColor': '#000000', 'signalTextColor': '#000000', 'labelBoxBorderColor': '#000000', 'labelBoxBkgColor': '#ffffff', 'labelTextColor': '#000000', 'loopBkgColor': '#ffffff', 'noteBkgColor': '#ffffff', 'noteTextColor': '#000000', 'noteBorderColor': '#000000'}}}%%
sequenceDiagram
    box rgb(220, 240, 220)
    participant Src as Источник данных
    end
    box rgb(230, 240, 255)
    participant Engine as Движок обработки
    end
    box rgb(255, 238, 220)
    participant Sink as Приёмник данных
    end
    box rgb(240, 220, 240)
    participant Offset as Хранилище смещений
    end

    Engine->>Src: Запрос данных пакета X
    Src-->>Engine: Данные пакета X
    Engine->>Sink: Запись результатов пакета X
    Note over Sink: Данные сохранены ✓

    rect rgb(255, 200, 200)
        Note over Src, Offset: ! СБОЙ — смещение пакета X не зафиксировано !
    end

    Engine->>Offset: Чтение контрольной точки
    Offset-->>Engine: Пакет X считается незавершённым
    Engine->>Src: Повторный запрос данных пакета X
    Src-->>Engine: Те же данные пакета X
    Engine->>Sink: Повторная запись пакета X

    rect rgb(255, 200, 200)
       
        Note over Sink: ! ДУБЛИКАТЫ !
    end